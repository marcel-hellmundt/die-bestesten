name: Deploy Asset Server

on:
  push:
    branches: ['**']
    paths: [asset_server/**]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || 'development' }}
    steps:
      - uses: actions/checkout@v4

      - name: Verify FTP_DIR_IMAGE and FTP_DIR_AUDIO are set
        run: |
          if [ -z "${{ vars.FTP_DIR_IMAGE }}" ]; then
            echo "FTP_DIR_IMAGE is not set for this environment — aborting to avoid mirroring to the FTP root."
            exit 1
          fi
          if [ -z "${{ vars.FTP_DIR_AUDIO }}" ]; then
            echo "FTP_DIR_AUDIO is not set for this environment — aborting to avoid mirroring to the FTP root."
            exit 1
          fi

      - name: Install lftp
        run: sudo apt-get install -y lftp

      - name: Upload to FTP
        run: |
          lftp -c "
            set ftp:ssl-force true;
            set ftp:ssl-protect-data true;
            set ssl:verify-certificate false;
            open -u ${{ secrets.FTP_USER }},${{ secrets.FTP_PASSWORD }} ${{ secrets.FTP_HOST }};
            mirror --reverse --verbose ./asset_server/ ${{ vars.FTP_DIR_IMAGE }};
            mirror --reverse --verbose ./asset_server/ ${{ vars.FTP_DIR_AUDIO }};
          "
